# Macro War Room History Feed

Public JSON feed of hourly Macro War Room live-risk buckets.

Canonical data file:

- `live-risk-history.json`

This repository stores aggregate risk-index history only. It does not contain credentials or user data.
